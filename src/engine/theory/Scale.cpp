#include "Scale.h"

namespace morph
{
// ScaleDefinition is fully constexpr in the header; this translation unit
// exists so the build graph and future scale tables have a stable home.
} // namespace morph
